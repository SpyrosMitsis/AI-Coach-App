.env
.env.local
.DS_Store
node_modules/
**/build/
**/.next/
supabase/.branches/
supabase/.temp/
*.log

# build/tooling artifacts
*.tsbuildinfo
*.apk
*.aab
.gradle/
local.properties

# personal data exports (test fixtures live in android/app/src/*/resources)
/strong*.csv
/notes*.md

# personal machine defaults for scripts/dev.sh (project ref, device serial, …)
scripts/dev.local.sh
.vercel

# Eval runs: JSONL + raw model output. Regenerable via scripts/dev.sh eval:run,
# grows without bound, and the raw text is large.
eval_runs/
.ipynb_checkpoints/
