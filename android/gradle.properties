org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# SUPABASE_URL / SUPABASE_ANON_KEY come from android/local.properties (see the
# secret() helper in app/build.gradle.kts). Don't put placeholders here — they
# would silently override a missing local.properties.

android.suppressUnsupportedCompileSdk=35
